### 推荐规则ppt栅格对齐 ###
将第二页「数据从哪来，谁负责什么」从碎片化的组件列表，重构为一条清晰的横向数据流程图，让产品/运营受众一眼看懂数据链路。

# 第二页「系统架构」重构为数据流程图

## Proposed Changes

### 整体布局重构

#### [MODIFY] [推荐规则ppt.html](file:///Users/dengxinyang/Desktop/AI·Project/D2C/Qwen/5 项目PPT/推荐规则ppt.html)

**当前结构**（问题所在）：
- 左列：6个组件堆叠列表（用户/ChatClient/Orchestrator/FlightAPI/CabinKB/LLM）
- 右列：4个技术要点卡片（并行检索/图片安全/Block协议/固定槽位）
- 两列割裂，看不出数据流向

**新结构**：删除原有左右两列布局，改为**全宽横向流程图 + 底部说明条**：

```
┌─────────────────────────────────────────────────────────────────┐
│  标题：数据从哪来，谁负责什么                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [用户] ──→ [Orchestrator] ──→ [FlightAPI]  ──→ [LLM] ──→ [渲染] │
│                    │           [CabinKB]                         │
│                    └──────────────────────────────────────────── │
│                                                                  │
├──────────────┬──────────────┬──────────────┬────────────────────┤
│  ⚡ 并行检索  │  🔒 图片安全  │  📦 Block协议 │  🎯 固定槽位        │
│  一句话说明   │  一句话说明   │  一句话说明   │  一句话说明         │
└──────────────┴──────────────┴──────────────┴────────────────────┘
```

**具体改动**：

1. **顶部流程图区域**（占页面约55%高度）：
   - 5个节点横向排列：`用户` → `Orchestrator` → `FlightAPI / CabinKB`（双层）→ `LLM` → `ChatClient`
   - 节点之间用带箭头的线连接，箭头上标注数据类型（「需求」「并行检索」「报价+事实」「文案+Schema」「SSE Block」）
   - `FlightAPI` 和 `CabinKB` 垂直叠放，共享一个「数据层」分组框
   - `Orchestrator` 用绿色高亮，作为核心枢纽
   - `LLM` 用红色标注「只写文案」，强调职责边界

2. **底部说明条**（占页面约30%高度）：
   - 4个等宽卡片横向排列（`.g4` 四列栅格）
   - 保留：并行检索、图片安全、Block协议、固定槽位
   - 每个卡片只保留标题 + 一句话说明，去掉冗长描述

3. **新增 CSS**：
   - `.flow` 横向流程图容器
   - `.flow-node` 节点样式
   - `.flow-arrow` 箭头样式
   - `.flow-label` 箭头上的数据标注
   - `.g4` 四列栅格
   - `.data-layer` 数据层分组框

## Verification Plan

### Manual Verification
- 打开浏览器查看第二页，确认流程图节点和箭头正确渲染
- 确认底部4个说明卡片等宽对齐
- 确认翻页动画正常


updateAtTime: 2026/4/2 16:12:41

planId: c673f3a8-cf4d-4e33-b449-10f90550474e