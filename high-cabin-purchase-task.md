# 高舱购买场景任务书（Figma MCP 设计执行）

## 0. 本版基线（替身映射）

- 设计稿基线（Figma）：
  - [飞猪AI·机票帮帮（node-id=301:16900）](https://www.figma.com/design/k9mFMyoUaYjXyYtK1Mdx21/%E9%A3%9E%E7%8C%AAAI%C2%B7%E6%9C%BA%E7%A5%A8%E5%B8%AE%E5%B8%AE?node-id=301-16900&t=SX7kgqiVlbS6irum-4)
- 数据源基线（FlightData 替身语义）：
  - `/Users/dengxinyang/Desktop/AI·Project/FlightData`
- **Figma 链路与写入规范**（本项目默认）：
  - `/Users/dengxinyang/Desktop/AI·Project/D2C/Qwen/.cursor/skills/figma-link-workflow/SKILL.md`
  - 读稿：Figma MCP（`get_design_context` 等）；改画布：**`use_figma`**（调用前必读 **figma-use**）
- 历史自动化（可选，非默认）：`Qwen/scripts/vibma-polish-highcabin.js` 等依赖 Vibma Relay 的脚本，仅在需要批量跑旧链路时使用，**不**替代 Figma MCP 标准流程。

## 1. 任务背景

- 目标：基于飞猪国内机票场景，设计一个“用户购买高舱（商务舱）”的转化页面/模块。
- 设计意图：AI 需要尽可能清晰地介绍高舱价值，帮助用户从“看价格”转向“看价值”。
- 使用方式：本任务书后续将直接喂给 AI，通过 **Figma MCP** 解析上述设计链接并在画布上用 **`use_figma`** 执行设计（见 `figma-link-workflow` skill）。

## 2. 输入资料与数据口径

## 2.1 视觉参考

- 参考图（当前可用 1 张）：
  - `/Users/dengxinyang/.cursor/projects/Users-dengxinyang-Desktop-AI-Project-D2C-Qwen/assets/image-0909ff66-9920-40a1-8f85-84e5edfc871b.png`
- 参考图作用：
  - 作为“机票列表卡片 + 舱位卖点位”的版式参考
  - 不要求逐像素复刻，重点学习信息层级与阅读流

## 2.2 数据来源（仅允许使用已验证字段）

- 国泰航空（主数据样本）：
  - `/Users/dengxinyang/Desktop/AI·Project/FlightData/国泰航空 CX/A350-1000/机型详情.md`
- 新加坡航空（补充样本）：
  - `/Users/dengxinyang/Desktop/AI·Project/FlightData/新加坡航空 SQ/Airbus A350-900/机型详情.md`
  - `/Users/dengxinyang/Desktop/AI·Project/FlightData/新加坡航空 SQ/Airbus A350-900/V.1/机型详情.md`

数据约束：
- 不允许编造参数、评分、舱位数量。
- 如果字段缺失，必须显式标注“暂无可靠数据”，禁止脑补。
- 文案允许“提炼表达”，但不得改变事实含义。

## 3. 高舱卖点数据池（可直接用于文案）

## 3.1 国泰 A350-1000（优先引用）

- 商务舱：46 座，座椅宽度 20 英寸（51cm），间距 45-75 英寸（114-190cm），180 度全平躺。
- 综合评分：4.62/5（480 条评论）。
- 设施：Wi-Fi、USB 电源、机上娱乐系统、个人设备充电端口。
- 服务感知关键词：全平躺睡眠、私密性、精致餐饮、多道式服务、长途恢复更好。

## 3.2 新航 A350-900（补充“高舱策略差异”）

- A350-900 多版本：
  - ULR：161 座，商务舱 67，优选经济舱 94，无经济舱（超长航线）。
  - V.1：253 座，商务舱 42，优选经济舱 24，经济舱 187。
  - V.2.0：303 座，商务舱 40，无优选经济舱，经济舱 263。
- V.1 商务舱参数：腿部空间 60 英寸，座椅宽度 28 英寸，可 180 度平躺。
- V.1 评分：4.48/5（469 条评论）。

## 3.3 可用卖点模板（事实驱动）

- 空间卖点：全平躺 + 大腿部空间 + 更宽座椅。
- 休息卖点：夜航睡眠质量更高，到达后状态更好。
- 服务卖点：优先服务、餐食品质、乘务响应。
- 效率卖点：高价值旅客节省体力与决策成本。
- 信任卖点：可展示评分和评论量作为证据。

## 4. 页面目标与用户任务

## 4.1 业务目标

- 提升高舱详情点击率。
- 提升高舱选择率与下单转化率。
- 降低用户对“高价不值”的心理阻力。

## 4.2 用户任务

- 在机票列表中快速理解“高舱贵在哪里、值在哪里”。
- 在 1-2 屏内完成“对比 -> 认可 -> 点击购买/升级”。

## 5. 页面信息架构（IA）

建议结构（自上而下）：

1. 行程摘要区
- 出发/到达时间、时长、航司、是否中转、价格锚点。

2. 高舱价值主卡（核心转化区）
- 主标题：高舱价值一句话（如“全平躺商务舱，长途到达更有状态”）。
- 三个核心卖点标签：睡眠、空间、服务。
- 证据条：评分/评论数/可量化参数。

3. 舱位对比区（高舱 vs 其他）
- 至少对比 4 项：座椅宽度、腿部空间、平躺能力、服务等级。
- 对比必须同维度、同单位。

4. 场景化卖点区
- 夜航场景、商务出行场景、家庭出行场景的价值解释。

5. 信任证据区
- 引用用户评价摘要（正向为主，保留少量客观限制）。

6. 行动区（CTA）
- 主按钮：立即预订高舱/升级高舱。
- 次按钮：查看舱位详情/对比更多航班。

## 6. 文案策略与语气规范

- 语气：专业、可信、克制，不夸张。
- 句式：卖点 -> 证据 -> 行动。
- 禁止：
  - “绝对最好”“100%”等无法验证表达。
  - 将非高舱能力写成高舱独有能力。

示例文案骨架：
- 卖点：180 度全平躺，长途飞行也能深度休息。
- 证据：以国泰 A350-1000 为例，商务舱 46 座，评分 4.62/5（480 条评论）。
- 行动：立即查看高舱价格与可订座位。

## 7. Figma 执行要求（Figma MCP + use_figma）

执行前置：
1. 确认 Cursor 已连接 Figma MCP；用设计稿链接解析 `fileKey` / `nodeId` 后先 `get_design_context`（必要时 `get_metadata` / `get_screenshot`）。
2. 画布写入前阅读 **figma-use**，再通过 **`use_figma`** 落图层（详见 `Qwen/.cursor/skills/figma-link-workflow/SKILL.md`）。
3. 先输出页面区块树，再开始画布操作。
4. **不要**将 Vibma（3056/3055）列为本任务书的默认前置步骤。

组件与命名：
- 页面 Frame 命名：`HighCabin_Page_*`
- 模块命名：`Module_ValueCard`、`Module_Compare`、`Module_CTA`
- 组件命名：`Comp_HighCabin_Tag`、`Comp_HighCabin_CTA`

状态要求：
- 按钮至少 3 态：default、pressed、disabled。
- 可交互卡片至少 2 态：default、selected。

设计约束：
- 优先复用既有 design system 组件，不重复造轮子。
- 数据位与文案位分离，便于后续批量替换。
- 关键数据（评分/参数）必须可定位到数据来源注释。

## 8. H5 映射要求（设计到代码）

- 结构：`header/main/section/footer` 语义化。
- 响应式：优先适配 375 宽，兼容 320-430。
- 样式：沿用 `---ai-design.md` 中 token，避免魔法数色值。
- 交互：
  - CTA 可点击反馈
  - 对比区可折叠/展开
  - 弱网或数据缺失有降级展示

## 9. 可用素材指引

- 可使用 FlightData 目录下各机型 `images/` 作为候选素材。
- 优先选择：
  - 商务舱座椅图
  - 餐食图
  - 客舱氛围图
- 图片使用规则：
  - 一卡一主图，避免同屏多个视觉焦点竞争。
  - 素材与文案语义一致（餐食卖点配餐食图）。

## 10. 交付物清单

1. Figma 页面：
- 高舱购买主页面（至少 1 套）
- 核心模块组件化（至少 4 个模块）

2. 文案与数据清单：
- 每条关键卖点对应数据来源路径
- 争议字段标注“待核验”

3. H5 映射说明：
- 模块与代码结构映射表
- 交互事件定义（CTA、展开收起、对比切换）

4. 自检记录：
- 是否存在不可验证数据
- 是否覆盖关键状态
- 是否符合 design system 约束

## 11. 验收标准

- 卖点清晰：用户 5 秒内能看懂“高舱价值”。
- 信息可信：关键参数与评分均可追溯到数据源。
- 转化链路完整：从认知 -> 对比 -> 行动路径无断层。
- 可实现：交互和布局可直接映射到 H5，无不可落地设计。
- 可扩展：支持后续替换机型或航司数据，不需重做结构。

## 12. AI 执行指令（可直接复制）

```text
请基于《高舱购买场景任务书（Figma MCP 设计执行）》执行设计：
1) 先输出页面 IA 与模块树
2) 再输出每个模块的文案草案（卖点->证据->行动）
3) 标注每条证据的数据来源路径
4) 生成 Figma 设计执行清单（节点命名、组件、状态）
5) 输出 H5 映射结构（语义标签+交互点）

约束：
- 不编造数据
- 不使用绝对化宣传词
- 优先突出商务舱平躺、空间、服务、评分证据
```
