# Figma `181:52246` 与 PRD §6.4 对照（差距摘要）

**节点**：`机票`（750×4148），内含 `Feed·三方案连续区（规范）` `181:52304`。

## 已具备（对齐 PRD）

- 三方案槽位：经济 / 中转 / **方案三：舒适出行**（第三槽商务 upsell）。
- 方案三下 **类 Accordion**：`机舱布局`、`高端反鱼骨座椅`（含大图区 `ImageWithFallback` `181:52347`）、`娱乐设施`；右侧「文字链」可对应展开/收起。
- 底部 **大按钮** CTA 实例（`181:52361`）。
- 引导文案已提及国泰尔雅公务舱、平躺等（需改为 **可溯源至机型详情.md** 的表述）。

## 待补齐 / 修正

| 项 | 说明 |
|----|------|
| 大图区 | `181:52347` 为空容器，需嵌入 **FlightData CX** 座椅图（如 `2-座椅图片/09`）。 |
| 三缩略图 | 稿内方案三区块 **无** PRD 所述独立图集行；计划在 Accordion 主图下 **插入** 横向三图（`10/11/12`）。 |
| 文案事实 | `181:52356` 等长文案改为：46 座、20" 宽、45–75" 间距、180°、4.62/5（480 条）等 **仅来自机型详情** 的短列表式叙述。 |
| 机舱布局标题 | `181:52340` 与座位图一致：可附 **1-座椅布局/03** 缩略或文案注明「布局见图」。 |

## CabinUpsell 字段映射（实现参考）

- `facts[]`：商务舱座位数、宽度、间距、平躺、评分、评论数、Wi-Fi/USB/IFE。  
- `images[]`：`hero` → 09；`gallery[]` → 10,11,12；`seatmap` → 03。  
- `accordion[]`：`机舱布局` | `豪华商务舱（座椅）` | `娱乐与电源` | `用户评价摘要`（概括引用机型详情 §用户评价，不逐条伪造）。

---

## 执行状态（2026-03-29）

- **文案**：已通过 Figma MCP `use_figma` 写入 `181:52356` / `181:52335` / `181:52340`（国泰 A350-1000 可核验表述）。
- **大图 `181:52347`**：`get_screenshot` 显示容器内已有 Hero 位图；若需强制替换为 FlightData `seat-09` 链，可用仓库内 `Qwen/scripts/mcp-use-figma-cx-hero-args.json`（整段 `use_figma` 的 `arguments`）或 `figma-plugin-cx-hero-181-tiny.js.txt` 作为 `code` 字段来源（`use_figma` 的 `code` 上限 50000 字符）。
- **三缩略图行（Figma）**：未在本次自动插入；H5 Demo 已接本地 `seat-10`–`12` 作为 PRD 图集行参考。
